2-4
서버 프로그래밍을 위해서는 최소 2개 이상의 네트워크 포트가 필요하다.
내부 DB망용 하나, 외부 클라이언트들용 하나 이렇게 최소 2개

2-7
서버용 cpu 사용 이유 : 멀티 cpu를 지원하기 때문에

2-8
멀티 프로세서 : 씨피유 여러 개 꽂고 돌리는 거
그러면 예를 들어 cpu가 두 개 있을 때, 쓰레드를 100개 만들었다면 0번 1번 cpu가 50개씩 한다.
계산결과를 메모리에 올리기 전에 캐시에 가지고 있는데 그 때 다른 애가 그 메모리를 읽으려고 하면
캐시에 있는 거를 줘야한다.

2-9
과거 방식에서는 Memory controller가 bottle neck이 됐었다.
하지만 현대방식에서는 개선됐다.


2-11
멀티 프로세서(멀티 씨피유)보다 멀티코어가 성능적으로 좀 더 좋다

2-19
파이프라인이 많아진다고 속도가 빨라지지 않는 이유:
메모리 Read에 종속되기 때문에

따라서 메모리 읽기를 최대한 줄이고
캐시를 최대한 잘 사용해야한다.

SIMD
한 번에 여러개의 계산을 하자.
AVX512 - 512비트의 계산을 한번에 한다.
16개의 int를 한번에 더할 수 있는 효과

2-20
유일하게 사용하는 운영체제 시스템 콜이 네트워크다.
느려서 new, malloc 같은 운영체제 콜을 안 한다.
따라서 빠르게하기 위해 모든 데이타를 메모리에 올려놓고 쓴다.
하드디스크 접근 같은 것도 하지 않는다. 이것도 느려
이미 read한 데이터에 write할 때는 캐시미스가 나지 않는다.


2-24
우주에서 방사능이 떨어져서 그 입자들이 비트를 건드리면 
1.8시간당 기가바이트 당 1비트 정도 에러가 나는데
따라서 서버 메모리는 에러 수정 기능이 있고 비싸다.

2-32
이중루프를 돌 때, 캐시미스가 나지 않도록 같은 열을 최대한 접근하도록 해야함.
어떤 순서로 하는 지에 따라 큰 차이가 난다.














